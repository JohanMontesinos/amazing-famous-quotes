[project]
name = "famous-quotes"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Johan", email = "jmflip@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "pytest>=9.1.1",
]

[project.scripts]
famous-quotes = "famous_quotes:main"

[build-system]
requires = ["uv_build>=0.12.11,<0.13.0"]
build-backend = "uv_build"

[tool.pytest.ini_options]
pythonpath = ["."]
